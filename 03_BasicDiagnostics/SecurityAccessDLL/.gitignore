# The compiled SecurityAccessDLL.dll/.lib ARE tracked (committed on request,
# built with build_mingw.bat / MinGW-w64, confirmed working live in CANoe).
# Only leftover intermediate build files are ignored here.
*.exp
*.obj
*.pdb
*.ilk
self_test.exe
